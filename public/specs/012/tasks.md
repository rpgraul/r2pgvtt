# Tasks: Physics-First Refactoring

- [ ] **Task 1: Correções no GameState e Supabase (`gameState.svelte.ts` e `tables.ts`)**
  - Em `gameState.svelte.ts`, procure as funções `loadGameRole` e `checkUserGameMembership`. Altere a chamada do supabase `.single()` para `.maybeSingle()`.
  - Em `gameState.svelte.ts`, substitua a função `sendRoll` para aceitar 5 argumentos: `(formula, result, details, color, textual)`. Dentro dela, crie um objeto de chat (`chatMsg`) com o `textual` e tipo 'user', e um objeto `rollData`. Atualize os arrays locais (`this.rolls` e `this.chatMessages`), chame `this.roomChannel.send({ type: 'broadcast', event: 'dice_roll', payload: { roll: rollData, chatMsg, color } })` e salve tudo no banco de dados via `db`.
  - No `setupRoomChannel` (dentro de `gameState.svelte.ts`), no listener de `dice_roll`, adicione o `payload.chatMsg` ao array `this.chatMessages`, adicione `payload.roll` a `this.rolls` e chame `module.diceStore.playRemoteRoll(payload.roll)`.
  - No `src/lib/supabase/tables.ts`, garanta que `subscribeToChat` e `subscribeToRolls` NÃO usem `supabase.channel`. Eles devem apenas fazer um `select()` simples, chamar o callback `onInitialLoad` e retornar `() => {}`.

- [ ] **Task 2: Refatoração Central da Rolagem (`diceStore.svelte.js`)**
  - Modifique a função `rollDice(formula)`. O código interno que cria a Promise e roda a física (`diceBoxInstance.roll(...)`) deve ser encapsulado para podermos usar `await`. O fluxo será: `const result = await new Promise(...)` (onde roda a física 3D e resolve com os dados avaliados em `onRollComplete`). APÓS essa promise resolver, defina `const text = '🎲 Rolou ' + formula + ': ' + result.textual;`, em seguida importe o `gameState` dinamicamente e chame `m.gameState.sendRoll(formula, result.total, result, currentDiceColor, text);`. Por fim, retorne o `result`.
  - Implemente e exporte `playRemoteRoll(roll)`. Dentro desta função: capture a cor (`roll.color` ou `roll.details.color`), extraia `rawDetails = roll.details.details`, mapeie para o formato forçado `const forcedArray = rawDetails.map(d => ({ qty: 1, sides: roll.details.parsedData.sides, value: d.value, themeColor: color }));`. Torne `isDiceVisible = true`, chame `addRemoteAlert`, execute `diceBoxInstance.getInstance().roll(forcedArray)` ignorando erros, e use um `setTimeout` de 3000ms para `clear3DDice()`.

- [ ] **Task 3: Limpeza de Código Duplicado nas UIs**
  - No arquivo `src/components/dice/DiceRoller.svelte`: Substitua todo o bloco de código interno das funções `rollDice` e `rollCustom` para que contenham estritamente: `isLoading = true; try { lastResult = await diceStore.rollDice(formula); } catch(e) { console.error(e); } finally { isLoading = false; }`. Remova todas as chamadas `gameState.sendMessage` e `gameState.sendRoll`. Remova o elemento HTML `<div bind:this={diceContainer}...></div>` e remova a importação do `useDiceBox`.
  - No arquivo `src/components/chat/ChatSidebar.svelte`: Na linha que avalia `/r` e faz `match`, deixe apenas `diceStore.rollDice(formula).catch(...)`. Remova totalmente os blocos de `.then` que chamam `gameState.sendMessage` e `gameState.sendRoll`.
  - No arquivo `src/components/FAB.svelte`: Nas rolagens (como a case `d4`, `d6`, etc.), mantenha estritamente a chamada `diceStore.rollDice('1' + action).catch(...)`. Remova os comandos de `.then(...)` com `gameState`.